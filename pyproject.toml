[project]
name = "simulation-foundry"
version = "0.1.0"
description = "Simulation foundry"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.8",
    "pandas>=3.0.0",
    "pre-commit>=4.5.1",
]
